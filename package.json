{
  "name": "harsi-trading-platform",
  "version": "1.0.0",
  "private": true,
  "description": "HARSI AI Trading Platform — Institutional Algo Terminal, Signals, Paper Trading, and Execution Pipeline",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev": "concurrently -n \"API,WEB\" -c \"cyan,magenta\" \"npm --workspace=apps/api run dev\" \"npm --workspace=apps/web run dev\"",
    "dev:api": "npm --workspace=apps/api run dev",
    "dev:web": "npm --workspace=apps/web run dev",
    "build": "npm run build:packages && npm --workspace=apps/api run build && npm --workspace=apps/web run build",
    "build:packages": "npm --workspace=packages/shared run build && npm --workspace=packages/market-data run build && npm --workspace=packages/strategies run build && npm --workspace=packages/broker-adapters run build && npm --workspace=packages/trading-engine run build",
    "test": "jest --config jest.config.cjs --runInBand",
    "test:unit": "jest --config jest.config.cjs tests/unit",
    "test:strategies": "jest --config jest.config.cjs tests/strategies",
    "test:risk": "jest --config jest.config.cjs tests/risk",
    "test:api": "jest --config jest.config.cjs tests/api",
    "lint": "eslint . --ext .ts,.tsx,.js,.jsx --max-warnings=50 || true",
    "typecheck": "tsc -b --dry",
    "db:push": "npm --workspace=apps/api run db:push",
    "db:seed": "npm --workspace=apps/api run db:seed"
  },
  "devDependencies": {
    "@types/jest": "^29.5.12",
    "@types/node": "^20.14.0",
    "@types/supertest": "^7.2.1",
    "concurrently": "^8.2.2",
    "jest": "^29.7.0",
    "supertest": "^7.2.2",
    "ts-jest": "^29.1.5",
    "typescript": "^5.4.5"
  }
}
